.header {
  min-height: 100px
  padding-top: 55px
  padding-bottom: 20px
  display: flex
  box-sizing: border-box;
  align-items: center;
}

.header__controls {
  & > button:not(:last-child) {
    margin-right: 10px
  }
}

.header__search {
  margin-left: auto;
  position: relative;
}
